apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "earthmagneticfieldservice.fullname" . }}
  labels: {{- include "earthmagneticfieldservice.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels: {{- include "earthmagneticfieldservice.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations: {{- toYaml .Values.podAnnotations | nindent 8 }}
      labels:
        {{- include "earthmagneticfieldservice.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}{{ toYaml . | nindent 8 }}{{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "earthmagneticfieldservice.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - { name: ASPNETCORE_URLS, value: "http://+:8080" }
            - { name: EarthMagneticField__MaximumSamplesPerRequest, value: {{ .Values.earthMagneticField.maximumSamplesPerRequest | quote }} }
            - { name: EarthMagneticField__UsageStatisticsFile, value: {{ .Values.earthMagneticField.usageStatisticsFile | quote }} }
            - { name: EarthMagneticField__UsageStatisticsSaveIntervalSeconds, value: {{ .Values.earthMagneticField.usageStatisticsSaveIntervalSeconds | quote }} }
          ports:
            - { name: http, containerPort: 8080 }
          {{- if .Values.probes.enabled }}
          readinessProbe:
            httpGet: { path: /EarthMagneticField/api/health/ready, port: http }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /EarthMagneticField/api/health/live, port: http }
            initialDelaySeconds: 10
          {{- end }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
            - { name: statistics, mountPath: /home }
      volumes:
        - name: tmp
          emptyDir: {}
        - name: statistics
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ default (printf "%s-statistics" (include "earthmagneticfieldservice.fullname" .)) .Values.persistence.existingClaim }}
          {{- else }}
          emptyDir: {}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}

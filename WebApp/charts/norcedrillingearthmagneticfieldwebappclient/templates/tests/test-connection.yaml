apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingearthmagneticfieldwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingearthmagneticfieldwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingearthmagneticfieldwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
